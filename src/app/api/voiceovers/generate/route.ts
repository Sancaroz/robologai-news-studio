import { notImplemented } from "@/lib/api-placeholder";

/**
 * Phase 2 voiceover endpoint.
 *
 * This route must remain admin-triggered. It must not call a paid speech API
 * until a provider and billing boundary have been explicitly configured.
 */
export async function POST() {
  return notImplemented("English AI voiceover generation");
}
