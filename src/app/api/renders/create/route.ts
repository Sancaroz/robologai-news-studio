import { notImplemented } from "@/lib/api-placeholder";

/**
 * Phase 2 render endpoint.
 *
 * Planned output:
 * - 1080x1920 vertical video
 * - Short source excerpts selected for commentary
 * - Burned-in subtitles and title overlays
 * - Persistent source/company credit
 * - Generated narration
 *
 * A render creates a preview only. Publishing remains a separate,
 * admin-approved action and is intentionally outside the MVP.
 */
export async function POST() {
  return notImplemented("9:16 Shorts video rendering");
}
